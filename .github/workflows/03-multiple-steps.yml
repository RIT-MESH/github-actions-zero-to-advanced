name: "03 - Multiple Steps"
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/03-multiple-steps.yml"
permissions:
  contents: read
jobs:
  steps-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: List files
        run: ls -la
      - name: Greet
        run: echo "Step three complete"
