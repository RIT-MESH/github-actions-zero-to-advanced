# Exercise 13 — Secrets

Store a repository secret `FAKE_TOKEN` with the value `EXAMPLE_SECRET_VALUE`, then print only its length (never the value).

> See [`../solutions/13-secrets.md`](../solutions/13-secrets.md).